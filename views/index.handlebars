<div class="jumbotron">
  <h1 class="display-4">Bem Vindo ao Blog!</h1>
  <p class="lead">Este é um BLOG Simples, desenvolvido em Node.js + Express + MongoDB</p>
  <hr class="my-4">
  <p>Aqui você vai poder ler mais sobre as novidades do mundo da computação.</p>
  {{#if user}}
  {{else}}
      <a class="btn btn-primary btn-lg" href="/usuario/registro" role="button">Crie uma Conta</a>
  {{/if}}
</div>
<hr>
<h2>Postagens Recentes: </h2>
{{#each posts}}
    <div class="card mt-4">
        <div class="card-body">
            <h3>{{titulo}}</h3>
            <p>{{descricao}}</p>
            <a href="/post/{{slug}}"><button class="btn btn-success">Leia Mais</button></a>
            <hr>
            <small>Categoria: {{categoria.nome}}</small>
            <br>
            <small>Data de Publicação: {{date}}</small>
        </div>
    </div>
{{else}}
    <h5>Nenhuma Postagem Cadastrada</h5>
{{/each}}